# Compilador Seleccionado: gcc version 12.2.0 (MinGW en Windows)

Para compilar al programa utilice el siguiente comando (elegi el standard "c2x"):

```bash
gcc ./hello.c -std=c2x -pedantic-errors -o hello.exe
```
